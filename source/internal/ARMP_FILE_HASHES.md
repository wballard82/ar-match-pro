# ARMP File Hashes — ARMP-2026.07.21-PILOT-AUTH-R3

The complete, authoritative per-file hash list is `SHA256SUMS.txt` at the package root (verify with `sha256sum -c SHA256SUMS.txt`). Key deliverable hashes and the ZIP hash are recorded there and in the delivery message. This document is generated at package assembly so its hashes match the exact bytes shipped.
