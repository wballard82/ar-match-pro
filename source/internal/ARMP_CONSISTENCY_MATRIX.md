# Consistency Matrix (R3)

Cross-file claim alignment. ACH/wire only — consistent across index/app/admin/terms/privacy/generators; zero Stripe (gate-enforced). Server-controlled pilot access — consistent across app.html gate, admin console, Edge Functions, migrations. Demo Mode synthetic/no-expiry — consistent. Local financial boundary — consistent and instrumented. Internal-role protection — consistent between RLS and Edge Functions.
